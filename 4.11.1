def f(x, n):
    result = x ** n
    return result

x = 2
n = 3
print(f"{x} 的 {n} 次方是 : {f(x, n)}")

----------------------------------------

def f(x, n):
    result = x ** n
    return result

x = int(input("請輸入整數 x："))
n = int(input("請輸入整數 n："))

print(f"{x} 的 {n} 次方是 : {f(x, n)}")
